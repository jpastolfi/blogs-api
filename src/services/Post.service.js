/* const Post, Category = require('../models');

const insert = async (title, content, categoryIds) => {
  const allCategories = Category.findAll();
  const newPost = Post.create({ title, content, categoryIds });
  return newPost;
};

module.exports = {
  insert,
}; */